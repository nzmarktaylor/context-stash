# context-stash
As AI becomes a first‑class contributor to software projects, we need a shared memory layer that survives sessions, models, and tools. Context‑Stash is a Git‑native context system that lets AI agents write and retrieve reasoning, decisions, and intent — making AI‑augmented development safer and more reliable.
